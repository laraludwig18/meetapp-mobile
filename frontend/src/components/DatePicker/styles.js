import styled from 'styled-components';

export const Container = styled.label`
  background: rgba(0, 0, 0, 0.2);
  height: 50px;
  border-radius: 4px;
  margin-bottom: 10px;

  input#date-picker {
    background: none;
  }
`;
